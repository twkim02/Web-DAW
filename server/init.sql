-- 초기 데이터베이스 설정 (선택사항)
-- 이 파일은 MySQL 컨테이너가 처음 시작될 때 자동으로 실행됩니다.

-- 필요한 경우 여기에 초기 데이터나 스키마를 추가할 수 있습니다.
-- Sequelize의 sync()를 사용하는 경우 이 파일은 필요하지 않을 수 있습니다.

-- 예시: 추가 설정이 필요한 경우
-- SET GLOBAL sql_mode=(SELECT REPLACE(@@sql_mode,'ONLY_FULL_GROUP_BY',''));
