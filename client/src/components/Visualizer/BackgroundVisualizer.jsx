import React from 'react';
import ThreeVisualizer from './ThreeVisualizer';

const BackgroundVisualizer = (props) => {
    return <ThreeVisualizer {...props} />;
};

export default BackgroundVisualizer;
